camel:
  springboot:
    name: shopflow-firstroute-test
    main-run-controller: false

# In tests, the route's producer goes to a mock endpoint so we can assert on it.
ep:
  out: "mock:out"

app:
  demo:
    enabled: false   # keep the timer feeder OFF so tests are deterministic

logging:
  level:
    root: WARN
    com.example.eip: INFO
