camel:
  springboot:
    name: shopflow-translator-test
    main-run-controller: false

# In tests, the translator's output goes to a mock endpoint so we can assert on the canonical JSON.
ep:
  out: "mock:out"

app:
  demo:
    enabled: false   # keep the timer feeder OFF so tests are deterministic

logging:
  level:
    root: WARN
    com.example.eip: INFO
