camel:
  springboot:
    name: shopflow-aggregator-test
    main-run-controller: false

# In tests, the Aggregator's single output goes to a mock endpoint so we can assert on it.
ep:
  out: "mock:out"

app:
  demo:
    enabled: false   # keep the timer feeder OFF so tests are deterministic

logging:
  level:
    root: WARN
    com.example.eip: INFO
