camel:
  springboot:
    name: shopflow-channel-test
    main-run-controller: false

# In tests, the process route's terminal endpoint goes to a mock so we can assert on it.
ep:
  out: "mock:out"

app:
  demo:
    enabled: false   # keep the timer feeder OFF so tests are deterministic

logging:
  level:
    root: WARN
    com.example.eip: INFO
