camel:
  springboot:
    name: shopflow-enricher-test
    main-run-controller: false

# In tests, the enriched order lands on a mock endpoint so we can assert on its enriched fields.
ep:
  out: "mock:out"

app:
  demo:
    enabled: false   # keep the timer feeder OFF so tests are deterministic

logging:
  level:
    root: WARN
    com.example.eip: INFO
