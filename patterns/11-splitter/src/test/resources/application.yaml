camel:
  springboot:
    name: shopflow-splitter-test
    main-run-controller: false

# In tests, each split line item goes to a mock endpoint so we can count and inspect the fan-out.
ep:
  item: "mock:item"

app:
  demo:
    enabled: false   # keep the timer feeder OFF so tests are deterministic

logging:
  level:
    root: WARN
    com.example.eip: INFO
