camel:
  springboot:
    name: shopflow-exchange-test
    main-run-controller: false

# In tests, the route's terminal endpoint goes to a mock so we can assert on its headers.
ep:
  out: "mock:out"

app:
  demo:
    enabled: false   # keep the timer feeder OFF so tests are deterministic

logging:
  level:
    root: WARN
    com.example.eip: INFO
