camel:
  springboot:
    name: shopflow-messaging-test
    main-run-controller: false

# In tests, the Event Message destination goes to a mock endpoint so we can assert on it.
ep:
  event: "mock:event"

app:
  demo:
    enabled: false   # keep the timer feeder OFF so tests are deterministic

logging:
  level:
    root: WARN
    com.example.eip: INFO
